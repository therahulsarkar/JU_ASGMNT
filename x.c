#include <stdio.h>



int main()
{
    int arr [] = {1,4,6,3,7,0,56};

    int length = sizeof(arr)/sizeof(arr[0]);

    int start = 0, end = length-1;

    while(start<=end){
    	if(arr[start] < arr[end]){
    		
    	}
    }
    return 0;
}